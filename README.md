# usage:
    - countlines -h
    - countlines [options] file
    - countlines -r[options] <extensions> directory

# options:
    - t: only print total
    - T: don't print total
    - n: sort by name
    - r: recursive search
    - f: print just file names
    - p: print full paths
